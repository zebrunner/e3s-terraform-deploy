{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "StartBuild",
      "Effect": "Allow",
      "Action": [
        "codebuild:StartBuild"
      ],
      "Resource": [
        "arn:aws:codebuild:${region}:${account}:project/${codebuild_project_name}"
      ]
    }
  ]
}
